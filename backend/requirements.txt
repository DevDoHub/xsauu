# Web 框架
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.34.0

# 数据验证 & 配置
pydantic>=2.10.0
pydantic-settings>=2.8.0

# 数据库 (SQLModel = SQLAlchemy + Pydantic 一体化)
sqlmodel>=0.0.22
alembic>=1.15.0

# MQTT
paho-mqtt>=2.1.0

# HTTP 客户端（用于 mediamtx API 代理）
httpx>=0.27.0

# 日志
loguru>=0.7.0

# JWT 认证
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
python-multipart>=0.0.18

# SocketIO 兼容层（让旧 Jetson 边缘端能直接连新后端）
python-socketio[asyncio]>=5.11.0
